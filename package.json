{
  "name": "frontend-case",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:client": "cd client && npm install && npm run build",
    "build:server": "cd api && npm install && npm run build",
    "build": "run-s build:*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
